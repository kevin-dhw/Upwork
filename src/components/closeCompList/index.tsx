import React from "react";

const CloseCompList: React.FC = () => {
  return (
    <div className=" flex items-center rounded-md border hover:border-black">
      <div>CloseCompList</div>
    </div>
  );
};

export default CloseCompList;
